# Example environment variables for deployment
# Copy to .env.local (local dev) or configure in your hosting provider dashboard

# Application
APP_NAME=AI Assisted Web UI Starter
NODE_ENV=production

# Performance budgets (bytes / ms)
PERF_MAX_BUNDLE_SIZE=204800
PERF_MAX_LCP=2500
PERF_MAX_CLS=0.1
PERF_MAX_FID=100

# Telemetry / Analytics placeholders
# ANALYTICS_WRITE_KEY=
# SENTRY_DSN=

# API base (if deploying behind a sub-path or external API layer)
# NEXT_PUBLIC_API_BASE=https://your-api.example.com

# Feature flags
NEXT_PUBLIC_FEATURE_PERFORMANCE_DASHBOARD=true
NEXT_PUBLIC_FEATURE_EXPORTS=true

# Build metadata injected at build time (BUILD_TIME already set in next.config.js)
GIT_COMMIT_SHA=
